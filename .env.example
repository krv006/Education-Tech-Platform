# Django
SECRET_KEY=change-me-in-production
DEBUG=true
ALLOWED_HOSTS=*
TIME_ZONE=Asia/Tashkent

# Database — uncomment to use Postgres (docker compose up -d db)
# POSTGRES_DB=edtech
# POSTGRES_USER=edtech
# POSTGRES_PASSWORD=edtech_dev_password
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432

# CORS (frontend dev server)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# LiveKit (self-hosted, docker compose up -d livekit)
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=devsecret_change_me_32chars_min!
LIVEKIT_URL=ws://localhost:7880
